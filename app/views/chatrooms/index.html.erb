<h1>chats</h1>

<div>
  <div>
    <% @messages.each do |message| %>
      <ul>
      <%= message.chatroom.id %>
        <%= message.find_other_user(current_user).username %>
        <%= message.content %>
        <%= message.created_at %>
      </ul>
    <% end %>
  </div>
</div>
